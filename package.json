{
  "name": "fusion-mcp",
  "version": "1.0.0",
  "description": "An MCP server for Autodesk Fusion that runs on Cloudflare Workers",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "test": "pnpm run build && node dist/tests/basic.test.js"
  },
  "keywords": [
    "mcp",
    "autodesk",
    "fusion",
    "cloudflare-workers",
    "hono"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "forge-server-utils": "^9.0.5",
    "hono": "^4.11.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2",
    "wrangler": "^3.93.0"
  }
}
